import java.util.Arrays;

public class arrayList {
    public static void main(String[] args) {
        int[] numbers = {4,85,74,65,45};

        System.out.println(Arrays.toString(numbers));
        Arrays.sort(numbers);
        System.out.println(numbers);
        System.out.println(Arrays.toString(numbers));
        
    }
}
